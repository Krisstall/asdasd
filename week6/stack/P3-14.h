/*	================== destroyStack =================	This function releases all nodes to the heap.	   Pre  A stack	   Post returns null pointer*/LIST* destroyList (LIST* list){//	Local Definitions	LIST_NODE* temp;//	Statements	if (list)	   {	    // Delete all nodes in stack	    while (list->front != NULL)	       {	        // Delete data entry	        free (list->front->dataPtr);	        temp = list->front;	        list->front = list->front->link;	        free (temp);	       } // while	    // Stack now empty. Destroy stack head node.	    free (list);	   } // if stack	return NULL;}	// destroyStack