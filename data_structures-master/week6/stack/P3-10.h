/*	================== stackTop =================	Retrieves data from the top of stack without	 changing the stack.	   Pre  stack is a pointer to the stack	   Post Returns data pointer if successful	                null pointer if stack empty*/void* listfront (LIST* list){//	Statements	if (list->count == 0)	    return NULL;	else	    return list->front->dataPtr;}	// stackTop